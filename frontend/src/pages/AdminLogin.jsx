import React,{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{ShieldCheck,Eye,EyeOff}from'lucide-react';import{useAuth}from'../context/AuthContext';
export default function AdminLogin(){const{login,logout}=useAuth();const nav=useNavigate();const[form,setForm]=useState({email:'',password:''});const[error,setError]=useState('');const[loading,setLoading]=useState(false);const[show,setShow]=useState(false);const submit=async e=>{e.preventDefault();setError('');setLoading(true);const r=await login(form.email,form.password);setLoading(false);if(r.success){if(r.data.role!=='SUPER_ADMIN'){logout();setError('This page is for Super Admin accounts only.');return}nav('/admin')}else setError(r.message)};return <div className="min-h-screen bg-gradient-to-br from-rose-50 via-white to-peach-50 px-4 py-10 flex items-center"><div className="w-full max-w-lg mx-auto bg-white rounded-3xl border border-slate-100 shadow-lift p-7 sm:p-12"><div className="w-14 h-14 rounded-2xl bg-rose-100 text-rose-500 flex items-center justify-center"><ShieldCheck size={27}/></div><span className="eyebrow block mt-6">ShopSphere control room</span><h1 className="text-4xl font-display font-semibold mt-2">Admin Login</h1><p className="text-slate-500 mt-2">Sign in to manage staff, catalog and store operations.</p>{error&&<div className="mt-6 rounded-xl bg-rose-50 border border-rose-100 text-red-600 text-sm px-4 py-3">{error}</div>}<form onSubmit={submit} className="mt-7 space-y-5"><div><label className="field-label">Admin email</label><input className="input-field mt-2" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="admin@example.com"/></div><div><div className="flex justify-between"><label className="field-label">Password</label><Link to="/forgot-password" className="text-xs font-semibold text-brand-700">Forgot password?</Link></div><div className="relative mt-2"><input className="input-field pr-12" type={show?'text':'password'} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Enter your password"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-4 top-1/2 -translate-y-1/2 text-slate-400">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></div><button disabled={loading} className="btn-primary w-full h-12">{loading?'Signing in…':'Login to Admin'}</button></form><div className="mt-7 pt-5 border-t border-slate-100 flex justify-between text-sm"><Link to="/login" className="text-brand-700 font-semibold">Customer Login</Link><Link to="/employee/login" className="text-brand-700 font-semibold">Employee Login</Link></div></div></div>}
